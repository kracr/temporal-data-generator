PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX ace: <https://anonymous.com/AcademicConferenceEvent#>
PREFIX tweet: <https://anonymous.com/Tweet#>

SELECT ?name WHERE { 
  ?tweet tweet:mentionsPerson ?person.
  BIND(
    IF(CONTAINS(STR(?person), "#"), 
       STRAFTER(STR(?person), "#"), 
       STRAFTER(STR(?person), "/")
    ) AS ?name
  )
}
